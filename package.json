{
  "name": "currency-symbols",
  "version": "0.4.1",
  "description": "A tiny library providing mappings of currency codes to currency symbols",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run lint && ./node_modules/.bin/rollup -c",
    "lint": "./node_modules/.bin/eslint --ext js src test",
    "prepublishOnly": "npm run build",
    "test": "./node_modules/.bin/jest --coverage"
  },
  "author": "Paul Matthew Jaworski",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^6.6.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "jest": "^24.9.0",
    "rollup": "^1.26.3",
    "rollup-plugin-copy": "^3.1.0"
  }
}
